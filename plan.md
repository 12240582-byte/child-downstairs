# 小朋友下樓梯 (Child Downstairs) 開發計畫

## 1. 專案目標
開發一款基於網頁的垂直捲動動作遊戲，復刻經典遊戲「小朋友下樓梯」(NS-Shaft)。
此專案初期將專注於核心機制，並使用簡單的色塊（矩形）來代表玩家角色與各式幾何平台，確保遊戲順暢執行於現代瀏覽器。

## 2. 技術選型
- **標記語言**: HTML5 提供 `<canvas>` 畫布來繪製遊戲畫面。
- **邏輯實作**: Vanilla JavaScript (原生 JS)，負責遊戲狀態管理、物理運算與繪圖更新。
- **樣式控制**: CSS3，主要用於網頁版面配置與遊戲畫布的置中顯示。
- **運行環境**: 任何支援 HTML5 Canvas 的現代網頁瀏覽器即點即玩，無須依賴伺服器或建置工具（如可直接透過 Live Server 開啟）。

## 3. 核心遊戲機制

### 3.1 玩家角色 (Player)
- **外觀**: 藍色方塊。
- **控制**: 透過鍵盤的「左 / 右」方向鍵控制水平移動。
- **物理特性**: 
  - 具備重力（Gravity）下墜效果。
  - 當遇到平台時會觸發碰撞，並獲得與平台相同的向上的速度。

### 3.2 樓梯平台 (Platforms)
- **動態生成**: 定期從畫面底部生成新的平台，並以等速向畫面頂部移動。
- **銷毀機制**: 當平台完全移出畫面頂部時，將其從記憶體中移除。
- **平台種類**（初期先實作一般類，後續可擴充）：
  - **一般平台**: 綠色方塊，安全著陸點。
  - **輸送帶 (可選)**: 上去後會讓玩家自動往左或右滑動。
  - **針刺平台 (可選)**: 碰到會扣除生命值。
  - **彈簧平台 (可選)**: 碰到會將玩家向上彈飛。

### 3.3 遊戲勝負與狀態 (Game Rules)
- **生命值 (HP)**: 玩家預設擁有滿血生命值。
- **天花板傷害**: 畫面上方有隱形的「頂部針刺」，若玩家被平台推擠碰到畫面頂部，扣除大量生命值。
- **掉落深淵**: 若玩家完全掉出畫面底部，則直接判定 Game Over。
- **計分方式**: 根據存活時間或是已經往下掉落的層數 (Level) 來累加分數。

## 4. 階段實作步驟

### 階段一：建立環境與遊戲迴圈
1. 建立 `index.html`, `style.css`, 和 `main.js`。
2. 在 `index.html` 加入 `<canvas>` 元素並設定適當解析度（例如：400x600）。
3. 實作遊戲主迴圈 (`requestAnimationFrame`)，包含資料更新 (`update()`) 和畫面重繪 (`draw()`) 函數。

### 階段二：角色物理運動與控制
1. 定義 `Player` 類別（座標、寬高、速度等屬性）。
2. 在 `update` 中加上重力 (`y` 軸加速) 與地板邊界限制，讓主角可以自由落體。
3. 監聽 `keydown` 與 `keyup` 事件，實作角色左右平移。

### 階段三：平台生成與由上而下碰撞偵測
1. 定義 `Platform` 類別，負責以固定速度向上移動。
2. 實作定時生成平台的機制（需隨機變換水平位置與寬度）。
3. **核心碰撞**: 玩家向下掉落時，若其底部邊緣穿越平台上方邊緣，則將玩家 Y 軸位置鎖定在平台上，並清除其垂直速度。

### 階段四：遊戲狀態與介面完善
1. 實作上邊界（天花板天罰）和下邊界（無底洞）的死亡/扣血判定。
2. 加總下樓層數並顯示於畫面上。
3. 監控遊戲狀態，實作 `Game Over` 畫面，並允許按鍵重新啟動遊戲 (Restart)。
